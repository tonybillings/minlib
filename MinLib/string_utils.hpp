#pragma once

#include <string>
#include <vector>

std::vector<std::string> str_split(const std::string& str, char delimeter);

std::string str_ltrim(const std::string& str);

std::string str_rtrim(const std::string& str);

std::string str_trim(const std::string& str);